import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import Header from '@/components/layout/Header'
import Footer from '@/components/layout/Footer'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'LexLab - Expert Legal Solutions',
  description: 'Professional law firm providing comprehensive legal services with integrity, expertise, and dedication to achieving the best outcomes for our clients.',
  keywords: 'law firm, legal services, attorney, lawyer, legal consultation',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  )
}